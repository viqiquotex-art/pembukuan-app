// ==========================================
// NEXA - CLIENT SECURITY HARDENING
// ==========================================
// app.js is the single source of truth for transaction validation,
// CRUD, rendering, and UI behavior. This file is intentionally kept
// as a compatibility layer so older deployments can still load it
// without overriding app.js functions.
//
// Security-critical validation is also enforced server-side in worker.js.
(function () {
  'use strict';
})();
